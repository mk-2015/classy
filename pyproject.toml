[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "classy"
version = "1.0.0"
description = "A Python package used to make networking easier"
authors = [{ name="mk-2015", email="sxcyber17@gmail.com" }]
readme = "README.md"
license = { text = "MIT" }
dependencies = []
